import { Meteor } from 'meteor/meteor';
import '../imports/api/chatRooms';
import '../imports/api/chatMessage';
import '../imports/api/users';

Meteor.startup(() => {
  // code to run on server at startup
});
